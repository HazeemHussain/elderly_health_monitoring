<resources>
    <!-- Custom CalendarView Theme -->
    <style name="CustomCalendarTheme">
        <!-- Customize month title text color -->
        <item name="mcv_headerTextAppearance">@style/CustomHeaderTextAppearance</item>
        <!-- Customize weekday text color -->
        <item name="mcv_weekDayTextAppearance">@style/CustomWeekdayTextAppearance</item>
        <!-- Customize the date text color -->
        <item name="mcv_dateTextAppearance">@style/CustomDateTextAppearance</item>
    </style>

    <!-- Custom header text appearance for the calendar -->
    <style name="CustomHeaderTextAppearance" parent="TextAppearance.MaterialCalendarWidget.Header">
        <item name="android:textColor">#000000</item>
    </style>

    <!-- Custom weekday text appearance for the calendar -->
    <style name="CustomWeekdayTextAppearance" parent="TextAppearance.MaterialCalendarWidget.WeekDay">
        <item name="android:textColor">#000000</item>
    </style>

    <!-- Custom date text appearance for the calendar -->
    <style name="CustomDateTextAppearance" parent="TextAppearance.MaterialCalendarWidget.Date">
        <item name="android:textColor">#000000</item>
    </style>
</resources>
